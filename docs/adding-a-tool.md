# Adding a tool

Short path when the README isn’t enough.

1. Decide `scripts/` vs `tools/` (one file vs multi-file).
2. Implement with clear `--help` / usage.
3. Run it locally once.
4. Link the run command from the root README.
5. Open a PR with a one-line “why” in the description.
